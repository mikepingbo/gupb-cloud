<?xml version="1.0" encoding="UTF-8"?>
<configuration debug="true" scan="true" scanPeriod="30 seconds">
    <springProperty scope="context" name="logLevel" source="log.level"/>
    <springProperty scope="context" name="logPath" source="log.path"/>

    <!--输出到控制台-->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%level] [%class.%method:%line] - %m %n</pattern>
        </encoder>
    </appender>

    <!--输出到文件-->
    <appender name="FILE-OUT" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${logPath}/gupbcloud-gateway.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%level] [%class.%method:%line] - %m %n</pattern>
        </encoder>
        <!-- 按日期和大小切分生成日志文件 -->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${logPath}/gupbcloud-gateway.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <!--每100MB分隔-->
            <maxFileSize>100MB</maxFileSize>
            <!--最多存60个日志文件-->
            <!--<maxHistory>60</maxHistory>-->
            <!--最大存60G的日志文件-->
            <!--<totalSizeCap>20GB</totalSizeCap>-->
        </rollingPolicy>

    </appender>

    <root level="${logLevel}">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="FILE-OUT"/>
    </root>
</configuration>